# Core bot framework (supports Python 3.10-3.14)
python-telegram-bot>=20.7,<21.0

# Image processing (latest version for Python 3.14 support)
Pillow>=10.4.0,<12.0

# QR code generation
qrcode>=7.4.2

# QR code decoding (latest version)
pyzbar>=0.1.9

# HTTP client (required for python-telegram-bot)
httpx>=0.27.0

# ASGI server for webhooks
uvicorn>=0.30.0

# Web framework for health check endpoint
starlette>=0.37.0
